package com.java;

import java.util.Arrays;
import java.util.Collections;
import java.util.List;
import java.util.stream.Collectors;


public class Streamapi1 {

	public static void main(String[] args) {
		
		
		List<Integer> integers=Arrays.asList(1,3,7,9,8);
		
		//integers.stream().map(x -> x*2).forEach(System.out::println);
		
		//integers.stream().filter(x -> x>3).forEach(System.out::println);
		
		//List<Integer> sortedList=integers.stream().sorted().collect(Collectors.toList());
		//System.out.println(sortedList);
		
		//int num=integers.stream().min((x,y) -> x-y).get();
		//System.out.println(num);
		
		//int num1=integers.stream().max((x,y) -> x-y).get();
		//System.out.println(num1);
		
		
		
		
		
		
		
		
	

	}

}
